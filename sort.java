//going to call the word we are trying to insert "insertWord" for now

for(int h = 0; h < insertWord.length(); h ++){
    char insertWordLetter = insertWord.charAt(h);
}

for(int i = 0; i <arrayList.size()-1; i++){               //iterate through the arraylist
      String currentWord = arrayList.get(i);
      String nextWord = arrayList.get(i+1);                                                          
      for(int j = 0; j <currentWord.size(); j ++){              //how to turn string into arraylist so that I can scan them?
          char currentWordLetter = currentWord.charAt(j);          //if this is posisble
                                                //check first letter
        }  
        for(int k = 0; k <nextWord.size(); k ++){              
          char nextWordLetter = nextWord.charAt(k);
                                                
        }  
        if()
    
    
                                                
}